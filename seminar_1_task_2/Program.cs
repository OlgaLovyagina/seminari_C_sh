// пример задачи на ветвление
//если на улице идет дождь
 //то
 // возьми зонтик
 // иначе
 // можешь идти в сандалях
 int rain = 1; // 0 - не идет дождь, 1 - есть дождь
 if (rain == 0)
 {
    Console.WriteLine("возьми зонт");
 }
else
{
    Console. WriteLine("можешь идти в сандалях");
}
